apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quay-database
  labels:
    quay-component: postgres
  annotations:
    component: "postgres"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
