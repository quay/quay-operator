apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quay-postgres
  labels:
    quay-component: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
