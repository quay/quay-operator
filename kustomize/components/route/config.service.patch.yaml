apiVersion: v1
kind: Service
metadata:
  name: quay-config-editor
  labels:
    quay-component: quay-config-editor
  annotations:
    component: "route"
spec:
  type: ClusterIP
  selector:
    quay-component: quay-config-editor
